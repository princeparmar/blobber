# {{- $blobberCount := .Values.blobber.replicaCount | int }}
# {{- $blobberCount := add $blobberCount 1 | int }}
# {{- range $i := untilStep 1 $blobberCount 1 }}

# apiVersion: v1
# kind: PersistentVolume
# metadata:
#   name: blob-valid-data-pv-0{{ $i }}
#   labels:
#     tier: prod
# spec:
#   storageClassName: {{ $.Values.blobber.persistence.storageClass_valid_data }}{{ $i }}
#   capacity: 
#     storage: {{ $.Values.blobber.persistence.size_valid_data }}
#   accessModes: 
#     - {{ $.Values.blobber.persistence.accessMode_valid_data }}
#   hostPath:
#     path: "/mnt/kubernetes/blob-valid-data-pv-0{{ $i }}""

# {{- end }}
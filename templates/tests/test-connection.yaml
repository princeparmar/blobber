apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "0helm.fullname" . }}-test-connection"
  labels:
    {{- include "0helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "0helm.fullname" . }}:80']
  restartPolicy: Never

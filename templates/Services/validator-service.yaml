{{- $validatorCount := .Values.validator.validatorCount | int }}
{{- $validatorCount := add $validatorCount 1 | int }}
{{- range $i := untilStep 1 $validatorCount 1 }}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "0helm-blobber.labels" $ | nindent 4 }}-validator-0{{ $i }}
  name: {{ include "0helm-blobber.fullname" $ | nindent 4 }}-validator-0{{ $i }}
spec:
  ports:
    - name: "{{ $.Values.validator.validator_service.port }}{{ $i }}"
      port: {{ $.Values.validator.validator_service.port }}{{ $i }}
      targetPort: {{ $.Values.validator.validator_service.port }}{{ $i }}
  type: {{ $.Values.validator.validator_service.type }}
  selector:
    {{- include "0helm-blobber.selectorLabels" $ | nindent 6 }}-validator-0{{ $i }}

{{- end }}